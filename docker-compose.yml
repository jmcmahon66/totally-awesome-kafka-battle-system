version: '3'
services:
  app:
    build: .
    volumes: 
      - .:/app
  # hero:
  #   build: .
  #   volumes:
  #     - ./hero:/app
  # enemy:
  #   build: .
  #   volumes:
  #     - ./enemy:/app

  